server:
  port: 8761
  undertow:
    direct-buffers: true
spring:
  application:
    name: eureka-server
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    # 自我保护机制: 生产环境下必须设置为 true
    enable-self-preservation: false